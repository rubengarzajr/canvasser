{title}Adding an Image

{text}
There are several ways to add an image to Canvasser. We'll looks at several below:

{list}
  {item} Link to an image on using a URL
  {item} Add an image to your file system using the *File Manager*
  {item} Embed the image in the Canvasser file

{text} Linking an existing file with a URL:

{text} Adding an image to the file system:

{text} Embedding the image in the Canvasser file:
