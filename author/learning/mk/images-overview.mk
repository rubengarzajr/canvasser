{title}Images Overview

{text}
Images are *bitmap graphics* that Objects use to display pitures in Canvasser.

{text} Images can be in the following formats:
{list}
  {item} png
  {item} jpg
  {item} svg

{text} Select a link below to learn more:
{list}
  {item} {link} "images-add" Adding an image
  {item} {link} "images-path" Image paths
  {item} {link} "images-prop" Image properties
