{title}Image Paths

{text}
Images that are not embedded in the Canvasser file have two parts:

{list}
  {item} Name: This is the file name of image.
  {item} Path: This is the path to the image on the server.

{text} You can manage the paths by opening the *Path* window.
{text} Need to add show and center window!
{text}
<span class="learnlink" onclick="window.pickWin('pathcontents', 'togglepaths', 664, 'path')">Toggle Samples window</span>
